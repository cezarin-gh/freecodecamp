---
import "@fontsource/lato";

const { title } = Astro.props;
---

<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{title}</title>
    </head>
    <body>
        <slot />
    </body>
</html>

<style lang="scss">
    :root {
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            color: white;
            font-family: "Lato", sans-serif;
        }
    }

    body {
        background: #0a0a23;
    }
</style>